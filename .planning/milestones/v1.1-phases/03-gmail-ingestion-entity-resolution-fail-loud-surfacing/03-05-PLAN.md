---
phase: 03-gmail-ingestion-entity-resolution-fail-loud-surfacing
plan: 05
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/domain/review-queue.ts
  - src/domain/dead-letter.ts
  - src/domain/sync-state.ts
  - tests/domain/review-queue.test.ts
  - tests/domain/dead-letter.test.ts
  - tests/domain/sync-state.test.ts
autonomous: true
requirements: [REL-01, REL-02, REL-03]

must_haves:
  truths:
    - "A review item can be inserted (any of the three types) and listed by pending vs resolved status, ordered newest-first (REL-01)"
    - "A dead-letter item can be inserted (known_sender_failed or unparseable) and listed pending vs resolved, and a re-parse of the same message id flips its status to resolved (REL-02)"
    - "A sync run can be started (running) and finished (success|failed) with new/review/dead-letter counts, and the latest run is retrievable for the health indicator (REL-03)"
    - "getReviewCount / getDeadLetterCount return the pending counts the sidebar badges display"
    - "The ingested-messages ledger exposes isAlreadyIngested(messageId) and a tx-scoped recordIngested so the orchestrator dedups exactly once per message (DATA-06 / D3-06)"
  artifacts:
    - path: "src/domain/review-queue.ts"
      provides: "insertReviewQueueEntryTx/insertReviewQueueEntry, listPendingReviewItems, listResolvedReviewItems, resolveReviewItem"
      contains: "insertReviewQueueEntryTx"
    - path: "src/domain/dead-letter.ts"
      provides: "insertDeadLetterEntryTx/insertDeadLetterEntry, listPendingDeadLetter, listResolvedDeadLetter, resolveDeadLetterByMessageIdTx"
      contains: "resolveDeadLetterByMessageIdTx"
    - path: "src/domain/sync-state.ts"
      provides: "startSyncRun, finishSyncRun, getLatestSyncRun, getReviewCount, getDeadLetterCount, isAlreadyIngested, recordIngestedTx"
      contains: "isAlreadyIngested"
  key_links:
    - from: "src/domain/review-queue.ts"
      to: "src/db/validation.ts"
      via: "inserts validate through newReviewQueueEntryInput before Drizzle"
      pattern: "newReviewQueueEntryInput"
    - from: "src/domain/dead-letter.ts"
      to: "src/db/validation.ts"
      via: "inserts validate through newDeadLetterEntryInput before Drizzle"
      pattern: "newDeadLetterEntryInput"
  prohibitions:
    - "No delete/hard-remove function on review_queue or dead_letter — items only transition pending → resolved (fail-loud audit trail, REL-02)"
    - "The *Tx variants open NO transaction of their own (mirror appendStatusEventTx) so the orchestrator can compose one transaction per message (Pitfall 2)"
    - "Every insert validates through its zod schema before Drizzle — no raw unvalidated write (V5)"
---

<objective>
Turn on the Phase-1 stub tables with a fail-loud routing domain layer: CRUD for `review_queue` (REL-01) and `dead_letter` (REL-02), sync-run lifecycle + pending counts for the health indicator (REL-03), and the `ingested_messages` dedup ledger (DATA-06 / D3-06). This is pure domain CRUD over the 03-01 schema — no Gmail dependency — so it ships in Wave 2 in parallel with OAuth.

Vertical-slice value: this is the persistence half of "nothing is ever silently dropped" — every message the orchestrator (03-06) classifies lands in exactly one queue via these functions, and the sidebar health counts read from here.

Purpose: Provide the tx-composable primitives the orchestrator needs (one transaction per message) and the read helpers the queue UIs (03-08/03-09) and health surfacing (03-10) consume.
Output: `src/domain/{review-queue,dead-letter,sync-state}.ts` + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-RESEARCH.md
@.planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-PATTERNS.md
@src/domain/contacts.ts
@src/domain/events.ts
@src/db/schema.ts
@src/db/validation.ts
@tests/helpers/db.ts
</context>

## Artifacts this phase produces (this plan)

- `src/domain/review-queue.ts`: `insertReviewQueueEntryTx(tx,input)`, `insertReviewQueueEntry(db,input)`, `listPendingReviewItems(db)`, `listResolvedReviewItems(db)`, `resolveReviewItem(db,id,status)`
- `src/domain/dead-letter.ts`: `insertDeadLetterEntryTx(tx,input)`, `insertDeadLetterEntry(db,input)`, `listPendingDeadLetter(db)`, `listResolvedDeadLetter(db)`, `resolveDeadLetterByMessageIdTx(tx,messageId)`
- `src/domain/sync-state.ts`: `startSyncRun(db)`, `finishSyncRun(db,id,result)`, `getLatestSyncRun(db)`, `getReviewCount(db)`, `getDeadLetterCount(db)`, `isAlreadyIngested(db,messageId)`, `recordIngestedTx(tx,messageId,outcome,applicationId?)`
- Tests: review-queue / dead-letter / sync-state

<tasks>

<task type="auto">
  <name>Task 1: review-queue.ts + dead-letter.ts CRUD (+ Tx variants) + tests</name>
  <read_first>
    - src/domain/contacts.ts (createContact insert 16-25; list/filter pattern 100-110)
    - src/domain/events.ts (appendStatusEventTx non-transaction-owning shape + doc comment 6-37)
    - src/db/validation.ts (newReviewQueueEntryInput, newDeadLetterEntryInput from 03-01)
    - .planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-PATTERNS.md (review-queue/dead-letter CRUD 94-150; Tx-vs-wrapper discipline 311-314)
  </read_first>
  <action>
    Create `src/domain/review-queue.ts`: `insertReviewQueueEntryTx(tx, input)` validating with newReviewQueueEntryInput then tx.insert(reviewQueue).values(validated).run(), returning Number(lastInsertRowid) — opens NO transaction (mirror appendStatusEventTx's doc comment). `insertReviewQueueEntry(db, input)` public wrapper wrapping the Tx variant in one db.transaction. `listPendingReviewItems(db)` selecting where status = "pending" ordered by createdAt desc; `listResolvedReviewItems(db)` where status != "pending". `resolveReviewItem(db, id, status)` (status one of confirmed|reassigned|rejected) setting status + resolvedAt = new Date(). Create `src/domain/dead-letter.ts` with the same shape: `insertDeadLetterEntryTx`/`insertDeadLetterEntry` (validate newDeadLetterEntryInput), `listPendingDeadLetter`/`listResolvedDeadLetter` (status pending vs not), and `resolveDeadLetterByMessageIdTx(tx, messageId)` updating status="resolved" + resolvedAt where sourceMessageId = messageId AND status = "pending" (used when a later re-parse of the same message succeeds). Provide NO delete function on either table. Write tests/domain/review-queue.test.ts and tests/domain/dead-letter.test.ts using createTestDb(): insert each type, assert pending vs resolved listing + ordering, assert resolve transitions status (never deletes), and assert resolveDeadLetterByMessageIdTx flips a pending row to resolved.
  </action>
  <verify>
    <automated>npx vitest run tests/domain/review-queue.test.ts tests/domain/dead-letter.test.ts --exclude '**/.claude/**' && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "insertReviewQueueEntryTx" src/domain/review-queue.ts` >= 1; `grep -c "resolveDeadLetterByMessageIdTx" src/domain/dead-letter.ts` >= 1.
    - Neither file contains a delete/hard-remove: `grep -Ec "\.delete\(" src/domain/review-queue.ts src/domain/dead-letter.ts` returns 0.
    - Tests prove insert → pending list → resolve → resolved list (no row disappears).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Review-queue and dead-letter CRUD exist with tx-composable inserts, status-only resolution, and no delete path.</done>
</task>

<task type="auto">
  <name>Task 2: sync-state.ts — sync-run lifecycle, counts, and dedup ledger</name>
  <read_first>
    - src/domain/contacts.ts (insert/list CRUD shape)
    - src/db/schema.ts (syncRuns + ingestedMessages from 03-01)
    - .planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-RESEARCH.md (Pattern 3 dedup ledger 268-289; Don't Hand-Roll idempotency 314)
    - .planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-PATTERNS.md (sync-state role-match 22; schema ledger sketch 154-184)
  </read_first>
  <action>
    Create `src/domain/sync-state.ts`. Sync-run lifecycle: `startSyncRun(db)` inserting a syncRuns row (status "running") and returning its id; `finishSyncRun(db, id, { status, newCount, reviewCount, deadLetterCount, errorMessage? })` updating finishedAt = new Date() + status + counts; `getLatestSyncRun(db)` selecting the most recent by startedAt desc (for the health indicator). Counts: `getReviewCount(db)` = count of reviewQueue where status = "pending"; `getDeadLetterCount(db)` = count of deadLetter where status = "pending". Dedup ledger: `isAlreadyIngested(db, messageId)` returning whether an ingestedMessages row exists for messageId; `recordIngestedTx(tx, messageId, outcome, applicationId?)` inserting an ingestedMessages row (outcome one of transition|review|dead_letter) with onConflictDoNothing on the message_id unique index so a retried sync never double-records. Validate the sync-run write via newSyncRunInput. Write tests/domain/sync-state.test.ts: start→finish updates counts+status; getLatestSyncRun returns the newest; getReviewCount/getDeadLetterCount reflect pending rows; recordIngestedTx then isAlreadyIngested returns true and a duplicate record is a no-op.
  </action>
  <verify>
    <automated>npx vitest run tests/domain/sync-state.test.ts --exclude '**/.claude/**' && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec "startSyncRun|finishSyncRun|getLatestSyncRun|getReviewCount|getDeadLetterCount|isAlreadyIngested|recordIngestedTx" src/domain/sync-state.ts` returns 7.
    - Tests prove the run lifecycle, both counts, and ledger dedup (duplicate recordIngested is a no-op).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Sync-run lifecycle, pending counts, and the idempotent dedup ledger all exist and are tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator → DB | Untrusted parsed-email-derived values written to queue/ledger tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | review_queue / dead_letter / sync_runs inserts | high | mitigate | All writes go through zod schemas + Drizzle parameterized queries — no raw SQL interpolation of email-derived text |
| T-03-14 | Repudiation | fail-loud audit trail | high | mitigate | No delete function on either queue (grep-verified) — items only transition pending → resolved, preserving the never-silently-discard record (REL-02) |
| T-03-15 | Tampering | ingested_messages dedup | medium | mitigate | Unique index on message_id + onConflictDoNothing so a re-run cannot double-record or corrupt routing (DATA-06 / D3-06) |
</threat_model>

<verification>
- All three test files pass; `npx tsc --noEmit` clean.
- No `.delete(` on queue tables (grep). Ledger dedup is a no-op on duplicate.
</verification>

<success_criteria>
The review, dead-letter, sync-run, and dedup-ledger domain functions exist with tx-composable inserts, status-only resolution (no delete), pending counts, and idempotent dedup — the persistence contract every downstream ingestion/UI/health plan consumes.
</success_criteria>

<output>
Create `.planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-05-SUMMARY.md` when done.
</output>
