---
phase: 03-gmail-ingestion-entity-resolution-fail-loud-surfacing
plan: 09
type: execute
wave: 4
depends_on: ["03-05", "03-08"]
files_modified:
  - src/app/dead-letter/page.tsx
  - src/components/dead-letter-item.tsx
  - src/components/ui/scroll-area.tsx
autonomous: true
requirements: [REL-02]

must_haves:
  truths:
    - "The /dead-letter page lists unparseable mail and known-sender parse failures in a table with a Type badge, Sender/Subject, Date, and a View raw email action — nothing is silently discarded (REL-02)"
    - "The known_sender_failed type badge is destructive-red (D3-04 flag); the generic unparseable badge is neutral/secondary"
    - "View raw email opens a dialog whose bounded-height scroll-area shows the full raw payload as escaped plain text with no truncation"
    - "Dead-letter items have no user-facing delete/dismiss — they only move to Resolved when a later re-parse of the same message id succeeds (domain-driven)"
  artifacts:
    - path: "src/app/dead-letter/page.tsx"
      provides: "Server Component dead-letter table with Pending/Resolved tabs + 25/page Load more"
      contains: "listPendingDeadLetter"
    - path: "src/components/dead-letter-item.tsx"
      provides: "client View-raw-email dialog rendering the payload as escaped plain text in a scroll-area"
      contains: "use client"
  key_links:
    - from: "src/app/dead-letter/page.tsx"
      to: "src/domain/dead-letter.ts"
      via: "reads listPendingDeadLetter / listResolvedDeadLetter (from 03-05)"
      pattern: "listPendingDeadLetter|listResolvedDeadLetter"
  prohibitions:
    - "No delete/dismiss/ignore control on dead-letter items (REL-02 never-silently-discard; Phase 2 no-destructive-action precedent)"
    - "The raw email payload renders as escaped plain text only (pre/text node) — never via React's dangerous innerHTML API, regardless of whether the source MIME was HTML (untrusted external content)"
    - "dead-letter-item.tsx is a client component and must not import the db client"
  ui_considerations:
    - "empty: Dead-letter Pending tab at zero renders 'Nothing in the dead-letter queue' + 'No unparseable mail or known-sender failures to show — that's a good sign.'"
    - "empty: Resolved tab at zero renders 'Nothing resolved yet' + 'Items you confirm, create, or attach will show up here.'"
    - "populated: mixed types sorted newest-first; the type badge distinguishes unparseable vs known-sender-failed"
    - "overflow: the raw email content uses a bounded-height scroll-area with full text preserved (no truncation) — diagnostic fidelity over compact layout"
    - "overflow: large item counts use 25/page Load more"
    - "zero-one-many: 1 row and many rows use identical row layout"
    - "long-text: the full raw email body wraps inside the scroll-area with no character limit, rendered as escaped plain text (security-relevant)"
    - "focal point (visuals FLAG): each row's View raw email action is the row focal point; when rendered icon-only it carries an aria-label"
    - statement: "Initial table load renders shadcn Skeleton row placeholders (consistent with 03-08 / Phase 2)"
      verification: backstop
    - statement: "A list-fetch failure reuses Phase 2's 'Couldn't load this page. Refresh to try again.'"
      verification: backstop
    - statement: "Subject/sender text truncates with ellipsis in the row; full value via title attribute + the raw-email dialog"
      verification: backstop
---

<objective>
Make every parse failure visible (REL-02, D3-04). Build the `/dead-letter` Server Component table (Type badge — destructive-red for known_sender_failed, neutral for unparseable — Sender/Subject, Date, View raw email) reusing the 03-08 table/tabs/pagination pattern, plus a client dialog whose bounded scroll-area shows the full raw payload as escaped plain text. Items are never deletable and only leave Pending when a later re-parse of the same message id resolves them (domain behavior from 03-05).

Vertical-slice value: after this plan the user can SEE exactly which mail the system couldn't parse (and why), so template drift on a known sender is noticed and fixed rather than silently swallowed — the core fail-loud guarantee made visible.

Output: `/dead-letter` page + raw-email viewer.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-CONTEXT.md
@.planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-UI-SPEC.md
@.planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-PATTERNS.md
@src/domain/dead-letter.ts
@src/app/review/page.tsx
@src/components/contact-conversation-form.tsx
@src/components/board-column.tsx
</context>

## Artifacts this phase produces (this plan)

- `src/app/dead-letter/page.tsx` (Server Component)
- `src/components/dead-letter-item.tsx` (client — View raw email dialog)
- shadcn: `src/components/ui/scroll-area.tsx`

<tasks>

<task type="auto">
  <name>Task 1: /dead-letter Server Component table + tabs + pagination + empty states</name>
  <read_first>
    - src/domain/dead-letter.ts (listPendingDeadLetter / listResolvedDeadLetter from 03-05)
    - src/app/review/page.tsx (03-08 — reuse the same Table + Tabs + Load-more structure)
    - .planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-UI-SPEC.md (Surface 3 lines 74-82; Copywriting empty states 156-157; Color destructive rules 139-143)
  </read_first>
  <action>
    Add shadcn scroll-area: `npx shadcn@latest add scroll-area` (reuse table + tabs already added in 03-08). Create `src/app/dead-letter/page.tsx` as a Server Component reading listPendingDeadLetter(db) + listResolvedDeadLetter(db), mirroring the 03-08 review page: Tabs Pending/Resolved, a Table with columns Type (Badge — variant destructive when type === "known_sender_failed" per D3-04, variant secondary/neutral for "unparseable"), Sender/Subject (ellipsis + title attribute), Date, Actions (renders the client DeadLetterItem "View raw email"). Sort newest-first. Show the confirmed empty-state copy per tab. Paginate 25/page with Load more. Add a Skeleton loading fallback consistent with 03-08. Pass each row (including rawPayload, sender, subject, type, date) to the client item component.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const s=require('fs').readFileSync('src/app/dead-letter/page.tsx','utf8');if(!/listPendingDeadLetter/.test(s)){process.exit(1)}if(!/Nothing in the dead-letter queue/.test(s)){console.error('missing empty copy');process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - /dead-letter renders a Table + Pending/Resolved Tabs; reads listPendingDeadLetter/listResolvedDeadLetter.
    - known_sender_failed rows use the destructive badge variant; unparseable rows use neutral.
    - Empty-state copy present verbatim; 25/page Load more caps the initial render.
    - No delete control on the page (`grep -Ec "delete|dismiss|ignore" src/app/dead-letter/page.tsx` returns 0 for action controls).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>The dead-letter queue lists both failure types (known-sender flagged red) in a paginated, tabbed table with correct empty states and no delete path.</done>
</task>

<task type="auto">
  <name>Task 2: View raw email dialog (escaped plain text in a bounded scroll-area)</name>
  <read_first>
    - src/components/contact-conversation-form.tsx (client dialog + escaped free-text render conventions)
    - .planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-UI-SPEC.md (Surface 3 raw viewer rule 80; long-text/overflow considerations 195-200)
    - .planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-PATTERNS.md (escaped raw-content rendering 331-334)
  </read_first>
  <action>
    Create `src/components/dead-letter-item.tsx` ("use client"). Render the "View raw email" action (a button; if icon-only, add an aria-label "View raw email"). On click, open a shadcn Dialog whose body is a shadcn ScrollArea (bounded height) containing the full rawPayload rendered as escaped plain text inside a pre element / text node — never through React's dangerous innerHTML API, and with NO truncation of the raw content (diagnostic fidelity; only the table Subject column truncates). There is no resolve/delete/dismiss button in this dialog — dead-letter items are resolved only by a later successful re-parse (domain concern), so the dialog is read-only. Do not import the db client. Handle a null/empty rawPayload gracefully (show a short "raw payload unavailable" note rather than a crash).
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const s=require('fs').readFileSync('src/components/dead-letter-item.tsx','utf8');if(!/use client/.test(s)){process.exit(1)}if(/@\/db\/client/.test(s)){process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - dead-letter-item.tsx begins with "use client", imports no db client, and opens a ScrollArea dialog for the raw payload.
    - The dangerous innerHTML API is absent: `grep -v "^\s*//" src/components/dead-letter-item.tsx | grep -c "dangerouslySetInnerHTML"` returns 0.
    - No resolve/delete/dismiss control in the dialog (read-only viewer).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>The raw-email viewer shows the full payload as escaped plain text in a bounded scroll-area, read-only, with no truncation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored raw email → raw viewer | The most-untrusted content in the app (a payload that failed parsing) is displayed verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Tampering (stored XSS) | dead-letter-item.tsx raw viewer | high | mitigate | Raw payload rendered as escaped plain text in a pre/text node — dangerous innerHTML API absent (grep-verified); this is the highest-risk render surface in the phase (a payload that already failed to parse) |
| T-03-14 | Repudiation | dead-letter page/dialog | high | mitigate | No delete/dismiss — items resolve only via re-parse of the same message id, preserving the never-discard record (REL-02, grep-verified) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; empty-state copy present; destructive badge only for known_sender_failed; no delete on page/dialog; no dangerous innerHTML (grep).
</verification>

<success_criteria>
Every unparseable and known-sender-failed email is visibly listed (known-sender flagged red), its full raw payload viewable as escaped plain text, with no way to silently delete or dismiss it — REL-02 satisfied.
</success_criteria>

<output>
Create `.planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-09-SUMMARY.md` when done.
</output>
