---
phase: 03-gmail-ingestion-entity-resolution-fail-loud-surfacing
plan: 07
type: execute
wave: 4
depends_on: ["03-03", "03-04"]
files_modified:
  - src/gmail/parsers/workday.ts
  - src/gmail/parsers/ashby.ts
  - src/gmail/parsers/index.ts
  - tests/gmail/parsers/workday.test.ts
  - tests/gmail/parsers/ashby.test.ts
autonomous: true
requirements: [ING-02, ING-04]

must_haves:
  truths:
    - "The Workday parser extracts company + status + real event date from a synthetic Workday fixture, matching on the broad myworkday.com domain rather than one fixed sender address (Pitfall 5)"
    - "The Ashby parser extracts company + status + real event date from a synthetic Ashby fixture"
    - "dispatchParser routes Workday and Ashby From domains to their parsers, completing the 2-3 senders ING-04 requires; a Workday-domain email that fails the parser returns null (→ dead_letter known_sender_failed, never dropped)"
  artifacts:
    - path: "src/gmail/parsers/workday.ts"
      provides: "parseWorkday — broad-domain, per-tenant-tolerant Workday extraction (D3-05 gated)"
      contains: "parseWorkday"
    - path: "src/gmail/parsers/ashby.ts"
      provides: "parseAshby — Ashby extraction (D3-05 gated)"
      contains: "parseAshby"
    - path: "src/gmail/parsers/index.ts"
      provides: "dispatchParser extended to Workday + Ashby domains"
      contains: "parseWorkday"
  key_links:
    - from: "src/gmail/parsers/index.ts"
      to: "src/gmail/parsers/workday.ts"
      via: "dispatchParser matches a myworkday.com From domain and delegates to parseWorkday"
      pattern: "parseWorkday"
  prohibitions:
    - "Extraction is regex-only — no LLM/AI call (D3-01)"
    - "The Workday match must be broad (myworkday.com family), not a single fixed sender address — otherwise real Workday mail is under-captured and never even reaches dead-letter (Pitfall 5)"
    - "Neither parser defaults the event date to received-time — real event date or null (D3-05)"
---

<objective>
Layer breadth onto the proven pipeline: add the Workday and Ashby per-sender parsers (D3-02) so ING-04's "2-3 real senders" is met (Handshake from 03-06 + these two). Workday is multi-tenant, so match on the broad `myworkday.com` family and tolerate per-employer template variation (Pitfall 5) — routing anything that fails the D3-05 bar to dead_letter rather than dropping it. Parsers are authored against the synthetic Workday/Ashby fixtures captured during 03-03 sampling; they are validated end-to-end by the live smoke test in 03-06.

Vertical-slice value: after this plan the user's next Sync now parses Workday and Ashby mail into transitions too, not just Handshake — broadening real coverage while keeping the same fail-loud routing.

ASSUMPTION (surface explicitly): Workday/Ashby templates are LOW confidence (D3-02); regexes reflect the sampled fixtures and MUST be spot-checked in the 03-06 live smoke test.
Output: `src/gmail/parsers/{workday,ashby}.ts`, extended dispatch, and unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-CONTEXT.md
@.planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-RESEARCH.md
@.planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-03-SUMMARY.md
@src/gmail/parsers/index.ts
@src/gmail/parsers/handshake.ts
@src/db/validation.ts
</context>

## Artifacts this phase produces (this plan)

- `src/gmail/parsers/workday.ts`: `parseWorkday(msg)`
- `src/gmail/parsers/ashby.ts`: `parseAshby(msg)`
- `src/gmail/parsers/index.ts`: dispatch extended to route Workday (broad myworkday.com) + Ashby
- Tests: `tests/gmail/parsers/workday.test.ts`, `tests/gmail/parsers/ashby.test.ts`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parseWorkday (broad-domain, per-tenant-tolerant) + dispatch + tests</name>
  <read_first>
    - src/gmail/parsers/handshake.ts (structure to mirror — regex extraction returning a parsedEmailResult or null)
    - src/gmail/parsers/index.ts (dispatchParser + KNOWN_SENDER_DOMAINS)
    - .planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-RESEARCH.md (Pitfall 5 Workday 347-351; Assumption A3)
    - .planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-03-SUMMARY.md (synthetic Workday fixtures + confirmed sender rule)
  </read_first>
  <behavior>
    - A synthetic Workday status fixture yields { company, stageLabel, occurredAt, roleTitle? } with a real event date.
    - A Workday-domain fixture missing company/status/date yields null (→ orchestrator routes to dead_letter known_sender_failed).
    - dispatchParser(anySubdomain.myworkday.com From) returns the Workday parser (broad match), not just one exact address.
  </behavior>
  <action>
    Create `src/gmail/parsers/workday.ts` exporting `parseWorkday(msg)` mirroring the Handshake parser: regex over subject+text to extract company, a status keyword → stage label, and a real event date; validate via parsedEmailResult; return null below the D3-05 bar. Write the domain match to be broad — any host ending in `myworkday.com` (the family), not a single fixed address — since Workday is per-tenant (Pitfall 5). Extend `dispatchParser` in `src/gmail/parsers/index.ts` so a From host in the myworkday.com family routes to parseWorkday. Mark the regexes as LOW-confidence execution-time assumptions in a comment. Write tests/gmail/parsers/workday.test.ts covering a parse success, a below-bar null, and a broad-domain dispatch match across two different myworkday.com subdomains.
  </action>
  <verify>
    <automated>npx vitest run tests/gmail/parsers/workday.test.ts --exclude '**/.claude/**' && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "parseWorkday" src/gmail/parsers/workday.ts` >= 1 and dispatchParser routes myworkday.com to it.
    - Test proves broad-domain dispatch across two subdomains and a below-bar null.
    - No LLM import (`grep -rEc "anthropic|openai|ollama" src/gmail/parsers` returns 0).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Workday mail parses (or fails to null) and dispatches on the broad myworkday.com family.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: parseAshby + dispatch + tests</name>
  <read_first>
    - src/gmail/parsers/handshake.ts (structure to mirror)
    - src/gmail/parsers/index.ts (dispatchParser after Task 1)
    - .planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-03-SUMMARY.md (synthetic Ashby fixtures + confirmed domain)
    - .planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-RESEARCH.md (Assumption A2 Ashby ashbyhq.com)
  </read_first>
  <behavior>
    - A synthetic Ashby status fixture yields { company, stageLabel, occurredAt, roleTitle? } with a real event date.
    - A below-bar Ashby fixture yields null.
    - dispatchParser(ashbyhq.com From) returns the Ashby parser.
  </behavior>
  <action>
    Create `src/gmail/parsers/ashby.ts` exporting `parseAshby(msg)` mirroring the Handshake/Workday parsers (regex extract company+status+real date, validate via parsedEmailResult, null below the D3-05 bar). Extend `dispatchParser` to route the ashbyhq.com domain to parseAshby. Mark regexes LOW-confidence. Write tests/gmail/parsers/ashby.test.ts covering parse success, below-bar null, and dispatch match.
  </action>
  <verify>
    <automated>npx vitest run tests/gmail/parsers/ashby.test.ts tests/gmail/parsers/workday.test.ts tests/gmail/parsers/handshake.test.ts --exclude '**/.claude/**' && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "parseAshby" src/gmail/parsers/ashby.ts` >= 1 and dispatchParser routes ashbyhq.com to it.
    - All three parser test files pass together (Handshake + Workday + Ashby).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Ashby mail parses (or nulls) and dispatches; all three senders ING-04 requires are covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gmail → parsers | Untrusted per-tenant Workday/Ashby email content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-12 | Information Disclosure | parsers | high | mitigate | Regex-only local extraction; no LLM/third-party call (grep-verified, D3-01) |
| T-03-17 | Denial of Service (silent recall) | Workday domain match | medium | mitigate | Broad myworkday.com family match so per-tenant Workday mail is fetched at all, then dead-lettered if unparseable rather than never captured (Pitfall 5) |
</threat_model>

<verification>
- All parser tests pass; `npx tsc --noEmit` clean; no LLM import in src/gmail/parsers (grep).
- Workday matches broadly across subdomains.
</verification>

<success_criteria>
Workday and Ashby join Handshake as parsed senders (ING-04, 2-3 senders), with broad Workday matching that keeps its per-tenant mail from silently escaping the pipeline.
</success_criteria>

<output>
Create `.planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-07-SUMMARY.md` when done.
</output>
