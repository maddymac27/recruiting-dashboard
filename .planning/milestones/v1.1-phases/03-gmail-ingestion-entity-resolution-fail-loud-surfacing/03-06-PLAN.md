---
phase: 03-gmail-ingestion-entity-resolution-fail-loud-surfacing
plan: 06
type: execute
wave: 4
depends_on: ["03-03", "03-04", "03-05"]
files_modified:
  - src/domain/ingestion.ts
  - src/app/actions.ts
  - src/components/ingestion-health.tsx
  - tests/domain/ingestion.test.ts
autonomous: false
requirements: [ING-03, ING-04, ING-06]

must_haves:
  truths:
    - "Triggering Sync now runs one sync end-to-end: a known Handshake email becomes a dated transition event attached to the matched application, visible on the board/detail (ING-04, ING-06)"
    - "Every fetched message lands in exactly one of {transition, review, dead-letter} and is recorded once in the ledger — nothing is silently dropped or double-counted"
    - "A known-sender email below the D3-05 bar routes to dead_letter flagged known_sender_failed with the raw email attached (D3-04); an unmatched known-sender email routes to review as unmatched_confirm_create (adopted default); an ambiguous match routes to review as low_confidence_match (conservative default)"
    - "The label pass backfills the Job Search label into review as label_mail, skipping any message id already ingested by the sender-query pass (D3-06 — query-pass wins)"
    - "Re-running a sync over the same messages creates no duplicate events or queue rows (DATA-06 idempotency)"
    - "syncGmailAction and Sync now are unavailable in demo mode"
  artifacts:
    - path: "src/domain/ingestion.ts"
      provides: "runGmailSync(db, client, { lastSync }) orchestrating both passes with per-message transactions and D3-05 routing"
      contains: "runGmailSync"
    - path: "src/app/actions.ts"
      provides: "syncGmailAction — demo-gated, records a sync_runs row, revalidates queues"
      contains: "syncGmailAction"
  key_links:
    - from: "src/domain/ingestion.ts"
      to: "src/domain/events.ts"
      via: "high-confidence transitions call appendStatusEventTx with the parsed real event date + sourceMessageId inside a per-message transaction"
      pattern: "appendStatusEventTx"
    - from: "src/domain/ingestion.ts"
      to: "src/domain/sync-state.ts"
      via: "isAlreadyIngested gate + recordIngestedTx per message enforce D3-06 dedup / query-pass precedence"
      pattern: "isAlreadyIngested|recordIngestedTx"
    - from: "src/app/actions.ts"
      to: "src/domain/ingestion.ts"
      via: "syncGmailAction calls runGmailSync between startSyncRun and finishSyncRun"
      pattern: "runGmailSync"
  prohibitions:
    - "ALL async work (Gmail fetch, MIME parse, regex, matching) completes BEFORE entering any db.transaction — no await inside a transaction callback (Pitfall 3)"
    - "One transaction PER MESSAGE, never one giant transaction for the whole run (Pitfall 2 / large D3-06 backfill safety)"
    - "A parsed status is never written to applications.currentStageId — transitions go only through appendStatusEventTx (D-09)"
    - "The event date is the parsed real-world date, never the email received-time (D3-05)"
    - "A message is never silently dropped — if it is not a transition it is written to review or dead_letter before recordIngested"
    - "syncGmailAction gates on dashboardMode === 'real' before any Gmail/token access (T-03-05)"
  ui_considerations:
    - "loading: manual sync in-progress renders the disabled button with 'Syncing…' label + Loader2 spin (Surface 1)"
    - "error: sync failure fires the toast 'Sync failed — nothing was imported. Try again, or check the dead-letter queue for details.' (persistent sidebar state lands in 03-10)"
---

<objective>
Compose the fetch pipeline (03-04) + routing domain (03-05) into one end-to-end manual sync (ING-06) that reliably parses Handshake into dated transition events (ING-04) and backfills the Job Search label (ING-03) — with strict routing discipline so nothing is silently dropped or double-counted (D3-04/D3-05/D3-06). Wire the Sync now button, record a sync_runs row, and confirm it against the real inbox via a live smoke test.

Vertical-slice value: this is the phase payoff — after this plan a real user clicks Sync now and a real Handshake email appears as a correctly-dated status transition on the pipeline board, while everything uncertain or unparseable is visibly queued rather than lost.

Adopted discretion defaults (CONTEXT flagged "confirm during planning"; adopted here as decisions, aligned with fail-loud + trust): unmatched email → review as unmatched_confirm_create (user approves creating the app); match confidence → conservative (only an exact/aliased company with a single open application and plausible progression auto-attaches; anything ambiguous → review).
Output: `src/domain/ingestion.ts`, `syncGmailAction`, wired Sync now button, and a confirmed live sync.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-CONTEXT.md
@.planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-RESEARCH.md
@.planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-PATTERNS.md
@.planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-UI-SPEC.md
@src/domain/applications.ts
@src/domain/companies.ts
@src/domain/events.ts
@src/domain/lookups.ts
@src/app/actions.ts
@src/components/ingestion-health.tsx
@tests/helpers/db.ts
</context>

## Artifacts this phase produces (this plan)

- `src/domain/ingestion.ts`: `runGmailSync(db, client, { lastSync })` returning `{ newCount, reviewCount, deadLetterCount }`; internal `matchApplication` + `classifyAndRoute` helpers
- `src/app/actions.ts`: `syncGmailAction()` (returns `{ ok:true, newCount, reviewCount } | { ok:false, error }`)
- Wired Sync now button in `src/components/ingestion-health.tsx`
- `tests/domain/ingestion.test.ts`

<tasks>

<task type="auto">
  <name>Task 1: runGmailSync orchestrator — both passes, D3-05 routing, D3-06 dedup, per-message tx</name>
  <read_first>
    - src/domain/applications.ts (quickSaveApplication 122-152 tx shape; getApplicationDetail; note board/open-application read)
    - src/domain/companies.ts (resolveCompany/createCompany)
    - src/domain/events.ts (appendStatusEventTx — call from inside the per-message tx; ON CONFLICT idempotency)
    - src/domain/lookups.ts (listStages — resolve stageLabel → stageId)
    - src/domain/sync-state.ts (isAlreadyIngested, recordIngestedTx from 03-05)
    - src/domain/review-queue.ts + dead-letter.ts (insert Tx variants from 03-05)
    - src/gmail/fetch.ts + parsers/index.ts + query.ts (from 03-04)
    - .planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-RESEARCH.md (System diagram 132-189; Pitfalls 2/3 330-340)
    - .planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-PATTERNS.md (ingestion.ts exact analog 35-90)
  </read_first>
  <action>
    Create `src/domain/ingestion.ts` exporting `runGmailSync(db, client: GmailClient, { lastSync }: { lastSync: Date | null })` returning counts. Sender-query pass FIRST: build q via buildSenderQuery(KNOWN_SENDER_DOMAINS, lastSync ?? undefined); listAllMessageIds({ q }); for each id, if isAlreadyIngested skip; else await fetchParsedMessage (async, BEFORE any transaction). Classify (all sync computation done pre-transaction): dispatchParser(msg.from) → if no parser, this is an unknown sender on the query pass → route dead_letter type unparseable; else result = parser(msg): if null (below D3-05 bar) → dead_letter type known_sender_failed with rawPayload = the raw message text + sender/subject; else resolve stageId via listStages by result.stageLabel and match an application via a matchApplication helper (resolveCompany then find OPEN applications for that company): exactly one open application with a plausible forward/terminal stage → high confidence; multiple candidates → low_confidence_match (pick best candidateApplicationId, set confidence); no company/application match → unmatched_confirm_create (carry parsedCompany/parsedRoleTitle/parsedStageLabel/parsedEventDate for prefill). THEN open ONE db.transaction for this message and, inside it (synchronous only): for a high-confidence transition call appendStatusEventTx({ applicationId, stageId, occurredAt: result.occurredAt, sourceMessageId: id, confidence }) then recordIngestedTx(tx, id, "transition", applicationId); for review insertReviewQueueEntryTx then recordIngestedTx(tx,id,"review"); for dead_letter insertDeadLetterEntryTx then recordIngestedTx(tx,id,"dead_letter"). Label pass SECOND (D3-06): resolveJobSearchLabelId(client); listAllMessageIds({ labelIds:[labelId] }) with no after: when lastSync is null (full historical backfill) else after:lastSync; for each id, if isAlreadyIngested skip (so a known-sender ATS mail already parsed in pass one is NOT also recorded as label_mail — query-pass wins); else await fetchParsedMessage, then per-message tx: insertReviewQueueEntryTx type label_mail with bodyText = verbatim msg.text, sender, subject, parsedEventDate = msg.date; recordIngestedTx(tx,id,"review"). Tally counts. Write tests/domain/ingestion.test.ts with makeFakeGmailClient + createTestDb covering: Handshake submitted → one transition + ledger transition; below-bar → dead_letter known_sender_failed; unmatched company → review unmatched_confirm_create; label-only message → review label_mail; query↔label overlap → recorded once (transition, no duplicate label review); re-run over same fixtures → no duplicate events/rows.
  </action>
  <verify>
    <automated>npx vitest run tests/domain/ingestion.test.ts --exclude '**/.claude/**' && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "runGmailSync" src/domain/ingestion.ts` >= 1; appendStatusEventTx is called with occurredAt from the parsed result (not received-time) and sourceMessageId = the message id.
    - No await appears inside any db.transaction callback in ingestion.ts (all fetch/parse is pre-transaction); `grep -Ec "transaction\(async" src/domain/ingestion.ts` returns 0.
    - ingestion.ts never writes applications.currentStageId directly (`grep -Ec "update\(applications\)[^)]*currentStageId" src/domain/ingestion.ts` returns 0).
    - Tests prove: exactly-one-of routing, query↔label dedup (query wins), and idempotent re-run.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>runGmailSync routes every message to exactly one of transition/review/dead-letter with per-message transactions, D3-05 gating, and D3-06 dedup — proven by tests.</done>
</task>

<task type="auto">
  <name>Task 2: syncGmailAction + wire the Sync now button</name>
  <read_first>
    - src/app/actions.ts (ActionResult 37-39; revalidatePath convention; existing dashboardMode usage none — add it)
    - src/db/client.ts (dashboardMode export)
    - src/domain/sync-state.ts (startSyncRun/finishSyncRun/getLatestSyncRun)
    - src/gmail/client.ts (getGmailClient) + src/gmail/oauth.ts (hasStoredToken)
    - src/components/ingestion-health.tsx (from 03-03 — the disabled Sync now button to wire)
    - .planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-UI-SPEC.md (Surface 1 states 50-56; Copywriting Contract sync success/failure 158-159)
  </read_first>
  <action>
    Add `syncGmailAction()` to `src/app/actions.ts`: import dashboardMode from "@/db/client"; if dashboardMode !== "real" return { ok:false, error:"Gmail sync is unavailable in demo mode." }; if !hasStoredToken() return { ok:false, error:"Gmail not connected yet." }; else compute lastSync = a successful getLatestSyncRun(db)?.finishedAt ?? null; const runId = startSyncRun(db); wrap runGmailSync(db, getGmailClient(), { lastSync }) in try/catch; on success finishSyncRun(db, runId, { status:"success", ...counts }), revalidatePath("/"), revalidatePath("/review"), revalidatePath("/dead-letter"), return { ok:true, newCount, reviewCount }; on throw finishSyncRun(db, runId, { status:"failed", errorMessage: String(err) }) and return { ok:false, error:"Sync failed — nothing was imported. Try again, or check the dead-letter queue for details." }. In `src/components/ingestion-health.tsx`, wire the Sync now button onClick (when connected): call syncGmailAction inside useTransition; while pending render disabled + "Syncing…" + Loader2 spin; on { ok:true } toast "Sync complete — {newCount} new, {reviewCount} to review." (sonner); on failure toast the returned error. Keep the demo-mode/not-connected gating from 03-03 intact.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const s=require('fs').readFileSync('src/app/actions.ts','utf8');if(!/syncGmailAction/.test(s)){process.exit(1)}if(!/dashboardMode/.test(s)){console.error('missing demo gate');process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - syncGmailAction exists, gates on dashboardMode !== "real", and records both a running start and a success|failed finish for the sync_runs row.
    - Sync now button shows the in-progress state (Syncing… + Loader2) and toasts success/failure with the confirmed copy.
    - `grep -c "revalidatePath" src/app/actions.ts` shows the sync action revalidates /, /review, /dead-letter.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Sync now triggers a demo-gated, sync_runs-recorded end-to-end sync and reflects progress + result in the sidebar.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live sync smoke test against the real inbox</name>
  <read_first>
    - .planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-RESEARCH.md (Sampling Rate / phase gate 486-489; Assumptions A1/A2/A3)
  </read_first>
  <action>
    Blocking human gate — run one live Sync now against the real inbox (mocked unit tests cannot prove the A1/A2/A3 domain/template assumptions). Following the verify steps, the human triggers a sync in DASHBOARD_MODE=real, confirms a real Handshake email became a correctly-dated transition using the REAL event date (not received-time, D3-05), verifies no message was silently dropped (every processed id lands as transition, review, or dead-letter — D3-06), and records the observed counts plus any KNOWN_SENDER_DOMAINS / parser-regex corrections in 03-06-SUMMARY.md. Do not proceed until a live sync is confirmed.
  </action>
  <what-built>The full manual sync pipeline (query pass + label pass) wired to the Sync now button. Mocked unit tests cannot prove the real Handshake/Workday/Ashby domain + template assumptions (A1/A2/A3) — one live run is required.</what-built>
  <how-to-verify>
    1. Run `DASHBOARD_MODE=real npm run dev`, ensure Gmail is connected, click Sync now.
    2. Confirm the success toast shows counts, and inspect: at least one real Handshake email became a dated transition on the correct application (board/detail) with the REAL event date (not received-time).
    3. Confirm the review queue and dead-letter counts are non-zero-as-expected and that nothing crashed the run; spot-check that a known-sender template that failed to parse landed in dead-letter (not silently dropped).
    4. If Handshake/Workday/Ashby domain or template assumptions were wrong (mail missing, or mis-parsed), record the correction needed and adjust KNOWN_SENDER_DOMAINS / the parser regex (small follow-up), then re-run.
    5. Record the observed counts + any template corrections in 03-06-SUMMARY.md.
  </how-to-verify>
  <acceptance_criteria>
    - A real Handshake email is visible as a correctly-dated transition after a live Sync now.
    - No message was silently dropped (every processed id appears as transition, review, or dead-letter).
    - 03-06-SUMMARY.md records the live-run counts and any template/domain corrections.
  </acceptance_criteria>
  <resume-signal>Type "approved" once a live sync is confirmed, or describe what mis-parsed/failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gmail → ingestion → DB | Untrusted email content classified and written to the event log / queues |
| demo mode ↔ real Gmail | Demo must never trigger a real sync |
| async I/O ↔ sync transaction | node:sqlite commits on callback return — async must not straddle the boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Denial of Service / Tampering | runGmailSync large backfill | high | mitigate | One transaction per message (Pitfall 2) so a mid-backfill failure preserves progress; all async before the transaction (Pitfall 3) so commits never race an await |
| T-03-16 | Tampering | classify/route discipline | high | mitigate | Every message written to exactly one of transition/review/dead_letter before recordIngested — no silent drop (tests assert exactly-one routing) |
| T-03-05 | Information Disclosure | syncGmailAction | high | mitigate | dashboardMode === "real" gate before any Gmail/token access |
| T-03-04 | Tampering | queue/event writes | high | mitigate | zod-validated + Drizzle-parameterized writes for all email-derived text |
</threat_model>

<verification>
- ingestion.test green (routing, dedup, idempotency); `npx tsc --noEmit` clean.
- No async-in-transaction and no direct currentStageId write (grep). Demo gate present.
- Live smoke test: a real Handshake transition with the real event date; nothing dropped.
</verification>

<success_criteria>
A manual Sync now pulls the targeted query + label, parses Handshake into correctly-dated transitions on the right application, routes everything else visibly to review/dead-letter, dedups query↔label overlap, and is idempotent on re-run — proven by tests and one live run.
</success_criteria>

<output>
Create `.planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-06-SUMMARY.md` when done.
</output>
