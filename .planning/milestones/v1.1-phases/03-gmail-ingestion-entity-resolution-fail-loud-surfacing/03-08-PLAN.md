---
phase: 03-gmail-ingestion-entity-resolution-fail-loud-surfacing
plan: 08
type: execute
wave: 3
depends_on: ["03-05"]
files_modified:
  - src/app/review/page.tsx
  - src/app/review/actions.ts
  - src/components/review-queue-item.tsx
  - src/components/ui/table.tsx
  - src/components/ui/tabs.tsx
autonomous: true
requirements: [REL-01]

must_haves:
  truths:
    - "The /review page lists pending review items in a table with a Type badge, Sender/Subject, parsed company/role, Date, and per-row Actions; a Pending/Resolved tab set separates active decisions from the resolved audit trail (REL-01)"
    - "A low_confidence_match row offers Confirm match / Choose different application; an unmatched_confirm_create row offers Create application (prefilled) / Attach to existing; a label_mail row offers Log as conversation (reusing the Phase 2 contact-conversation form)"
    - "Confirming, creating, attaching, or logging an item transitions it to Resolved (never deletes it) and appends the correct dated event / conversation via the existing domain write paths"
    - "Resolved items remain visible under the Resolved tab as a permanent audit trail"
  artifacts:
    - path: "src/app/review/page.tsx"
      provides: "Server Component review-queue table with Pending/Resolved tabs + 25/page Load more"
      contains: "listPendingReviewItems"
    - path: "src/app/review/actions.ts"
      provides: "confirmReviewMatchAction, createFromReviewAction, attachReviewToApplicationAction, logReviewAsConversationAction"
      contains: "resolveReviewItem"
    - path: "src/components/review-queue-item.tsx"
      provides: "client dialog dispatching per-type review actions"
      contains: "use client"
  key_links:
    - from: "src/app/review/actions.ts"
      to: "src/domain/review-queue.ts"
      via: "each review action resolves the item via resolveReviewItem after the domain write and revalidates /review + /"
      pattern: "resolveReviewItem"
    - from: "src/components/review-queue-item.tsx"
      to: "src/components/contact-conversation-form.tsx"
      via: "label_mail Log as conversation reuses the Phase 2 CAP-04 form prefilled with the email date + verbatim body"
      pattern: "ContactConversationForm|contact-conversation-form"
  prohibitions:
    - "No delete/discard/ignore control on review items — the only transitions are Pending → Resolved (audit trail)"
    - "Verbatim email body / parsed text renders as escaped JSX text only — never via React's dangerous innerHTML API (untrusted external content, carry the Phase 2 control)"
    - "review-queue-item.tsx is a client component and must not import the db client — data arrives as props, mutations go through Server Actions"
  ui_considerations:
    - "empty: Review queue Pending tab at zero renders 'Review queue is empty' + 'Every recent email matched cleanly…' (Copywriting Contract)"
    - "empty: Resolved tab at zero renders 'Nothing resolved yet' + 'Items you confirm, create, or attach will show up here.'"
    - "empty: confirm/create/attach dialogs pre-fill from parsed values when present, blank otherwise"
    - "error: a review confirm/create/attach action failure shows the reused Phase 2 copy 'Couldn't save this change. Check the fields below and try again.'"
    - "populated: high-volume backfill stays navigable via 25/page Load more + Pending/Resolved tabs"
    - "partial: an item with company+date but no role shows an em-dash (never literal null); the create dialog still requires company + role before save"
    - "overflow: large item counts use 25/page Load more, not unbounded scroll"
    - "zero-one-many: 1 row and many rows use identical row layout — only vertical extent + pagination change"
    - "focal point (visuals FLAG): each row's primary accent action (Confirm match / Create application / Log as conversation) is the row focal point; any icon-only control carries an aria-label"
    - statement: "Initial table load renders shadcn Skeleton row placeholders (consistent with Phase 2 board/timeline), no spinner"
      verification: backstop
    - statement: "A list-fetch failure reuses Phase 2's default 'Couldn't load this page. Refresh to try again.'"
      verification: backstop
    - statement: "An unmatched confirm&create item with no role title keeps role optional at save (matches CAP-01 quick-save partial rule)"
      verification: backstop
    - statement: "Subject/sender text truncates with ellipsis in the row, full value via title attribute + the row's own dialog"
      verification: backstop
---

<objective>
Make the review queue actionable (REL-01). Build the `/review` Server Component table (Type badge, Sender/Subject, parsed company/role, Date, Actions) with Pending/Resolved tabs and 25/page Load-more pagination for the D3-06 backfill, a per-type client dialog (confirm match / choose different / create-from-parsed / attach-to-existing / log-as-conversation), and the Server Actions that perform the domain write and transition the item to Resolved (never delete). Reuses Phase 2 primitives (add/edit dialog, contact-conversation form) rather than inventing new UI.

Vertical-slice value: after this plan the user can work through emails the system couldn't confidently place — confirming, creating, attaching, or logging each — closing the loop on "surfaced for review" (REL-01). Depends only on the routing domain (03-05), so it builds in Wave 3; it displays real items once a sync (03-06) populates the queue.

Output: `/review` page, review-queue-item dialog, and review actions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-CONTEXT.md
@.planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-UI-SPEC.md
@.planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-PATTERNS.md
@src/domain/review-queue.ts
@src/domain/applications.ts
@src/domain/contacts.ts
@src/domain/lookups.ts
@src/app/job/[id]/actions.ts
@src/components/contact-conversation-form.tsx
@src/components/board-column.tsx
</context>

## Artifacts this phase produces (this plan)

- `src/app/review/page.tsx` (Server Component)
- `src/app/review/actions.ts`: `confirmReviewMatchAction`, `createFromReviewAction`, `attachReviewToApplicationAction`, `logReviewAsConversationAction`
- `src/components/review-queue-item.tsx` (client)
- shadcn: `src/components/ui/table.tsx`, `src/components/ui/tabs.tsx`

<tasks>

<task type="auto">
  <name>Task 1: /review Server Component table + tabs + pagination + empty states</name>
  <read_first>
    - src/domain/review-queue.ts (listPendingReviewItems / listResolvedReviewItems from 03-05)
    - src/components/board-column.tsx (list + empty-state rendering shape; prop-threading; no client db import)
    - .planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-UI-SPEC.md (Surface 2 lines 59-72; Copywriting empty states 155-157; UI Considerations 176-201)
    - .planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-PATTERNS.md (Server Component list page 266-281)
  </read_first>
  <action>
    Add shadcn table + tabs: `npx shadcn@latest add table tabs`. Create `src/app/review/page.tsx` as a Server Component reading listPendingReviewItems(db) + listResolvedReviewItems(db). Render a shadcn Tabs (Pending / Resolved; active-indicator uses accent). Each tab shows a shadcn Table with columns: Type (Badge, variant secondary/neutral for all three review types — never destructive, per Color rules), Sender/Subject (truncate with ellipsis + title attribute for the full value), Parsed company/role (em-dash placeholder when absent — never literal null/undefined), Date, Actions (renders the client ReviewQueueItem). Show the confirmed empty-state copy for each tab when zero rows. Paginate at 25 rows/page with a Load-more button (simple count-based reveal, no numbered pages). Add a Skeleton-row loading fallback (via a loading.tsx or Suspense boundary consistent with Phase 2). Pass each row plus the stages/applications lookup data the dialog needs down as props (Server Component reads the db; the client dialog never imports it). Add a nav link/entry point to /review consistent with the sidebar count badge target (badge itself lands in 03-10).
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const s=require('fs').readFileSync('src/app/review/page.tsx','utf8');if(!/listPendingReviewItems/.test(s)){process.exit(1)}if(!/Review queue is empty/.test(s)){console.error('missing empty copy');process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - /review renders a Table + Pending/Resolved Tabs; `grep -c "listPendingReviewItems\|listResolvedReviewItems" src/app/review/page.tsx` >= 1.
    - Empty-state copy strings for both tabs are present verbatim; parsed-absent cells render an em-dash, not "null".
    - Load-more pagination caps the initial render at 25 rows.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>The review queue lists pending + resolved items in a paginated, tabbed table with correct empty states.</done>
</task>

<task type="auto">
  <name>Task 2: review actions (confirm / create / attach / log-as-conversation)</name>
  <read_first>
    - src/app/job/[id]/actions.ts (logContactAction/logConversationAction safeParse → domain write → revalidatePath shape)
    - src/domain/applications.ts (quickSaveApplication, updateApplication)
    - src/domain/events.ts (appendStatusEvent)
    - src/domain/contacts.ts (createContact, addConversation)
    - src/domain/review-queue.ts (resolveReviewItem)
    - src/domain/lookups.ts (listStages — resolve parsed stage label → stageId)
  </read_first>
  <action>
    Create `src/app/review/actions.ts` ("use server") with ActionResult-returning actions, each safeParsing input then composing existing domain writes and finally calling resolveReviewItem(db, id, status) and revalidatePath("/review") + revalidatePath("/"):
    - confirmReviewMatchAction(reviewId, applicationId): append a dated status event to applicationId via appendStatusEvent using the item's parsedStageLabel (→ stageId via listStages) + parsedEventDate + sourceMessageId; resolve item as "confirmed".
    - createFromReviewAction(reviewId, input): resolve/create company + create the application (quickSaveApplication/createApplication) using the item's parsed company/role/date, then append the parsed stage event; resolve item "confirmed". Require company + role non-empty (role optional falls back per CAP-01 partial rule — but require at save for create per UI-SPEC partial note).
    - attachReviewToApplicationAction(reviewId, applicationId): same as confirm but for an explicitly chosen existing application; resolve "reassigned".
    - logReviewAsConversationAction(reviewId, applicationId, input): create/reuse a contact + addConversation with the item's verbatim bodyText as the note and parsedEventDate as occurredAt, linked to applicationId; resolve "confirmed".
    Use the reused failure copy "Couldn't save this change. Check the fields below and try again." on validation failure. Never delete a review row.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const s=require('fs').readFileSync('src/app/review/actions.ts','utf8');if(!/resolveReviewItem/.test(s)){process.exit(1)}if(/\.delete\(/.test(s)){console.error('no delete allowed');process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - Four actions exist; each calls resolveReviewItem and revalidates /review + /.
    - No delete on review rows (`grep -Ec "\.delete\(" src/app/review/actions.ts` returns 0).
    - Each action composes existing domain writes (appendStatusEvent / quickSaveApplication / addConversation) — no new event-write path.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>All four review actions perform the correct domain write and transition the item to Resolved.</done>
</task>

<task type="auto">
  <name>Task 3: review-queue-item client dialog (per-type actions, escaped body)</name>
  <read_first>
    - src/components/contact-conversation-form.tsx (client dialog conventions: use client, useTransition, ActionResult handling, toast, escaped free-text render; Dialog primitives)
    - src/app/review/actions.ts (from Task 2)
    - .planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-UI-SPEC.md (Surface 2 row actions 65-68; Color accent/neutral rules 138-143)
  </read_first>
  <action>
    Create `src/components/review-queue-item.tsx` ("use client"). Given a review item + its type, render the Actions cell and the dialog(s) it opens, dispatching to the Task 2 actions inside useTransition. low_confidence_match → primary accent "Confirm match" (calls confirmReviewMatchAction with the suggested candidateApplicationId) + neutral/outline "Choose different application" (opens an application search/select reusing the add/edit dialog chrome → attachReviewToApplicationAction). unmatched_confirm_create → primary accent "Create application" (opens the quick-save-style dialog prefilled from parsed company/role/date → createFromReviewAction) + neutral "Attach to existing" (→ attachReviewToApplicationAction). label_mail → primary accent "Log as conversation" opening the existing ContactConversationForm prefilled with the email date + verbatim body → logReviewAsConversationAction. On { ok:false } setError + toast the failure copy; on ok toast success and let revalidation refresh the table. Render any verbatim email body / parsed snippet ONLY as escaped JSX text (a text node or a plain pre element) — never through React's dangerous innerHTML API. Do not import the db client. Add aria-labels to any icon-only control (visuals FLAG).
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const s=require('fs').readFileSync('src/components/review-queue-item.tsx','utf8');if(!/use client/.test(s)){process.exit(1)}if(/@\/db\/client/.test(s)){console.error('no db import in client');process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - review-queue-item.tsx begins with "use client", imports no db client, and renders the three per-type action sets.
    - The dangerous innerHTML API does not appear in the component: `grep -v "^\s*//" src/components/review-queue-item.tsx | grep -c "dangerouslySetInnerHTML"` returns 0.
    - `grep -c "Confirm match\|Create application\|Log as conversation" src/components/review-queue-item.tsx` >= 1.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Each review item type opens the smallest appropriate dialog and dispatches to the matching action, with untrusted body content rendered escaped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored email content → rendered UI | Untrusted sender/subject/body displayed in the review table + dialogs |
| client → Server Action | Review decisions mutate applications/events/conversations |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Tampering (stored XSS) | review-queue-item.tsx / page.tsx | high | mitigate | All email-sourced text (sender/subject/body/parsed) rendered as escaped JSX text only — dangerous innerHTML API absent (grep-verified); carries the Phase 2 control forward |
| T-03-14 | Repudiation | review actions | high | mitigate | No delete on review rows — items only transition Pending → Resolved, preserving the audit trail (grep-verified) |
| T-03-04 | Tampering | review actions | medium | mitigate | safeParse + existing zod-validated domain writes; no raw SQL |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; empty-state copy present; no delete on review rows; no dangerous innerHTML in the client component (grep).
- Actions call resolveReviewItem + revalidate.
</verification>

<success_criteria>
The review queue is a paginated, tabbed table where every uncertain email can be confirmed, created, attached, or logged via existing domain writes, transitioning to a permanent Resolved audit trail — with all untrusted content escaped and nothing deletable.
</success_criteria>

<output>
Create `.planning/phases/03-gmail-ingestion-entity-resolution-fail-loud-surfacing/03-08-SUMMARY.md` when done.
</output>
