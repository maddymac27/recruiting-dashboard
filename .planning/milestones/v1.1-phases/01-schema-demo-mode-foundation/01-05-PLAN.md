---
phase: 01-schema-demo-mode-foundation
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04"]
files_modified:
  - src/db/seed-lookups.ts
  - src/demo/seed/companies.ts
  - src/demo/seed/seed.ts
  - tests/db/seed.test.ts
  - tests/db/schema-parity.test.ts
  - package.json
autonomous: true
requirements: [DEMO-01, DEMO-02, DEMO-03]
user_setup: []

must_haves:
  truths:
    - "the canonical vocabularies (stages, sources, role types) are seeded idempotently into the DB as data, so role types stay extensible without a migration (D-05/D-06/D-07)"
    - "running the demo seed populates the demo store with ~15-20 invented (never-real) companies spread across every pipeline stage, with contacts and dated conversations (DEMO-01, D-12)"
    - "the seed writes only to the demo store via literal fixtures and has no code path that reads real.sqlite (DEMO-02, Pitfall 4)"
    - "the same migrations applied to two separate files produce identical schemas (DEMO-03)"
  artifacts:
    - src/db/seed-lookups.ts
    - src/demo/seed/companies.ts
    - src/demo/seed/seed.ts
    - tests/db/seed.test.ts
    - tests/db/schema-parity.test.ts
  key_links:
    - "seedDemo(db) reuses the domain write functions (createApplication, appendStatusEvent, createContact, addConversation) so demo data has correct projections"
    - "seedLookups is idempotent (insert-or-ignore by unique label) and used by both demo and real init"
    - "the seed CLI resolves the demo path from resolveDbPath('demo') only — never real"
---

<objective>
Seed the canonical domain vocabularies as data, generate the invented-company demo dataset spanning every pipeline stage, and prove the demo/real separation is structural: the seed only ever writes the demo store, and identical migrations produce identical schemas on both files.

Purpose: Completes DEMO-01/02/03 — the last of the 10 Phase 1 requirements — and makes the demo dataset dense enough that Phase 2's dashboard "looks alive" on a screen-share without exposing any real data (D-12/D-13).
Output: An idempotent lookup seeder, a literal invented-company fixture set, a demo seed script, and tests proving seed density, structural isolation, and schema parity.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-schema-demo-mode-foundation/01-CONTEXT.md
@.planning/phases/01-schema-demo-mode-foundation/01-RESEARCH.md
@.planning/phases/01-schema-demo-mode-foundation/01-VALIDATION.md
@.planning/phases/01-schema-demo-mode-foundation/01-01-SUMMARY.md
@.planning/phases/01-schema-demo-mode-foundation/01-02-SUMMARY.md
@.planning/phases/01-schema-demo-mode-foundation/01-03-SUMMARY.md
@.planning/phases/01-schema-demo-mode-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Idempotent vocabulary seeder (D-05/D-06/D-07)</name>
  <read_first>
    - .planning/phases/01-schema-demo-mode-foundation/01-RESEARCH.md (§Lookup Tables, D-05, D-06, D-07)
    - .planning/phases/01-schema-demo-mode-foundation/01-CONTEXT.md (D-05, D-06, D-07)
    - src/db/schema.ts, tests/helpers/db.ts
  </read_first>
  <files>src/db/seed-lookups.ts, package.json</files>
  <behavior>
    - seedLookups inserts the canonical stages, sources, and role types, and is idempotent (running it twice does not duplicate rows).
    - stages carry is_terminal and outcome_label so outcome derivation works (Offer/Rejected/Withdrawn/Ghosted terminal; Saved/Applied/Screen/Interview non-terminal).
  </behavior>
  <action>
    Create `src/db/seed-lookups.ts` exporting `seedLookups(db)` that inserts the locked vocabularies as DATA (insert-or-ignore by the unique label, so re-running is a no-op — this is why role types stay extensible without a migration, D-07):
    - stages (D-05): Saved, Applied, Screen, Interview, Offer, Rejected (terminal, outcome Rejected), Ghosted (terminal, outcome Ghosted), Withdrawn (terminal, outcome Withdrawn); Offer terminal with outcome Offer; Saved/Applied/Screen/Interview non-terminal (outcome null / Active).
    - sources (D-06): Handshake, Company site / ATS, Referral, LinkedIn, Job board / Other.
    - role_types (D-07): Product Management, Strategy, Chief of Staff, Other.
    Add a `db:seed:lookups` script to `package.json` (tsx entry that opens the DASHBOARD_MODE-resolved file and runs seedLookups) so the real store can be initialized the same way; keep the existing `db:seed:demo` script.
  </action>
  <verify>
    <automated>npx vitest run tests/db/seed.test.ts -t "lookups"</automated>
  </verify>
  <acceptance_criteria>
    - Running seedLookups twice against a test DB yields exactly one row per canonical label (idempotent).
    - All 8 stages, 5 sources, and 4 role types from D-05/D-06/D-07 are present with correct is_terminal/outcome_label on the terminal stages.
  </acceptance_criteria>
  <done>The locked vocabularies exist as seedable data, idempotently, keeping role types (and other vocabularies) extensible without a migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Invented-company demo dataset (DEMO-01, D-12)</name>
  <read_first>
    - .planning/phases/01-schema-demo-mode-foundation/01-RESEARCH.md (§Demo / Real Data-Source Swap, §Common Pitfalls Pitfall 4, D-12, D-13)
    - src/domain/applications.ts, src/domain/events.ts, src/domain/companies.ts, src/domain/contacts.ts (from 01-03/01-04)
    - src/db/paths.ts, src/db/migrate.ts, src/db/seed-lookups.ts
  </read_first>
  <files>src/demo/seed/companies.ts, src/demo/seed/seed.ts, tests/db/seed.test.ts</files>
  <behavior>
    - seedDemo(db) populates ~15-20 invented companies spread across every pipeline stage (a couple mid-interview, at least one Offer, several Ghosted, some Rejected, some Saved-not-applied), with a handful of contacts and dated conversations.
    - The dataset is built with the domain write functions so applications carry correct materialized current-stage projections.
    - seedDemo uses only literal fixtures; it never reads any real store.
  </behavior>
  <action>
    Create `src/demo/seed/companies.ts` exporting a literal fixture array of ~15-20 INVENTED company names (made-up, never real companies — D-12), each fixture specifying a target stage, a source, a role type, an optional date_applied (null for saved-not-applied), and optional contacts + dated conversations. Spread the fixtures so every stage is represented (a couple in Interview, >=1 Offer, several Ghosted, some Rejected, some Saved).
    Create `src/demo/seed/seed.ts` exporting `seedDemo(db)` that: calls `seedLookups(db)`, then for each fixture resolves-or-creates the company (createCompany/resolveCompany), creates the application (createApplication), and appends the status events (appendStatusEvent) needed to walk it to its target stage across plausible dates (so the derived current stage matches the fixture), plus creates any contacts (createContact + linkContactToApplication) and dated conversations (addConversation). Add a CLI wrapper (run via `tsx src/demo/seed/seed.ts` / `npm run db:seed:demo`) that resolves ONLY `resolveDbPath('demo')`, opens a `DatabaseSync`, runs migrations, and calls `seedDemo`. The seed must have NO reference to `resolveDbPath('real')` or any real store (Pitfall 4, D-13).
    Create `tests/db/seed.test.ts` covering: (a) "lookups" idempotency (Task 1); (b) "demo dataset" — run `seedDemo` against a fresh `createTestDb()` and assert >=15 companies exist, applications span >=5 distinct current stages including at least one Offer and at least one Ghosted, and at least one contact with >=1 dated conversation exists (DEMO-01, D-12).
  </action>
  <verify>
    <automated>npx vitest run tests/db/seed.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/db/seed.test.ts` exits 0.
    - "demo dataset" asserts >=15 invented companies across >=5 distinct stages (including Offer and Ghosted) with contacts and dated conversations (DEMO-01).
    - `src/demo/seed/seed.ts` contains no reference to the real store path (structural isolation, Pitfall 4).
  </acceptance_criteria>
  <done>The demo seed produces a dense, invented-only dataset across every stage using the real write path, and cannot touch real data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Structural isolation + schema parity across both files (DEMO-02, DEMO-03)</name>
  <read_first>
    - .planning/phases/01-schema-demo-mode-foundation/01-RESEARCH.md (§Demo / Real Data-Source Swap, D-13)
    - src/db/migrate.ts, src/db/paths.ts, tests/db/client.test.ts (from 01-02)
  </read_first>
  <files>tests/db/schema-parity.test.ts</files>
  <behavior>
    - Applying runMigrations to two separate temp SQLite files yields identical table sets (DEMO-03).
    - The demo and real paths are distinct, and no single client can address both (reinforces DEMO-02 established in 01-02).
  </behavior>
  <action>
    Create `tests/db/schema-parity.test.ts` that: opens two independent `DatabaseSync` files at distinct temp paths, runs `runMigrations` against each, reads each one's table list from sqlite_master, and asserts the two table sets are identical and both contain all 13 tables (DEMO-03 — migrations apply identically to demo and real). Add an assertion that `resolveDbPath('demo') !== resolveDbPath('real')` and that neither the seed nor the migrate CLI has a code path addressing both stores in one handle (DEMO-02). Clean up temp files.
  </action>
  <verify>
    <automated>npx vitest run tests/db/schema-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/db/schema-parity.test.ts` exits 0.
    - Test proves two separately-migrated files have identical 13-table schemas (DEMO-03).
    - Test reasserts demo/real paths are distinct (DEMO-02).
  </acceptance_criteria>
  <done>Migrations apply identically to both stores and the two data sources remain structurally separate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed fixtures -> demo store | Only literal invented data may enter the demo store |
| demo store vs real store | The two files must never be addressable in one session |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-18 | Information Disclosure | seed derived from real data | high | mitigate | seedDemo uses only literal invented fixtures; no code path reads real.sqlite (Pitfall 4, D-12); test asserts no real-path reference |
| T-01-19 | Information Disclosure | real data appearing in a demo/screen-share | high | mitigate | Structural two-file separation (DEMO-02) + invented-only company names (D-12); demo file also gitignored (01-01) |
| T-01-20 | Tampering | schema drift between demo and real | medium | mitigate | Same migrations applied to both; schema-parity test asserts identical table sets (DEMO-03) |
</threat_model>

<verification>
- `npx vitest run tests/db/seed.test.ts tests/db/schema-parity.test.ts` exits 0.
- Full suite `npx vitest run` green (all DATA-01..07 + DEMO-01..03 covered).
- `DASHBOARD_MODE=demo npm run db:seed:demo` populates `data/demo.sqlite` with the invented dataset.
</verification>

<success_criteria>
Toggling to demo mode points every query at a separate, seeded SQLite file (dense invented data across every stage) with no code path capable of mixing real and demo data, and migrations apply identically to both stores.
</success_criteria>

## Artifacts this plan produces

**Files:** `src/db/seed-lookups.ts`, `src/demo/seed/companies.ts`, `src/demo/seed/seed.ts`, `tests/db/seed.test.ts`, `tests/db/schema-parity.test.ts`; `package.json` (adds `db:seed:lookups` script).

**Symbols:** `seedLookups`, the invented-company fixture array, `seedDemo`.

**Data seeded:** stages (Saved, Applied, Screen, Interview, Offer, Rejected, Ghosted, Withdrawn), sources (Handshake, Company site / ATS, Referral, LinkedIn, Job board / Other), role_types (Product Management, Strategy, Chief of Staff, Other), and ~15-20 invented demo companies with applications, contacts, and conversations.

<output>
Create `.planning/phases/01-schema-demo-mode-foundation/01-05-SUMMARY.md` when done.
</output>
