---
phase: 05-analytics-dashboard-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - components.json
  - src/components/ui/chart.tsx
autonomous: false
requirements: [DASH-06]
user_setup: []
must_haves:
  truths:
    - "D5-07: recharts is installed only AFTER an explicit blocking-human package-legitimacy checkpoint (the same publisher/version vetting used for googleapis/mailparser in Phase 3, 03-01) — never auto-approved"
    - "The shadcn `chart` block is added from the official shadcn registry, creating src/components/ui/chart.tsx (ChartContainer/ChartConfig/ChartTooltip/ChartTooltipContent) and pulling recharts into package.json as a real dependency"
    - "recharts resolves to the official `recharts/recharts` package at v3.10.1 (peer react/react-dom ^19 satisfied by the installed react@19.2.8) with no postinstall script"
    - "The `progress` shadcn block is deliberately NOT installed this phase (RESEARCH Open Question 2 / UI-SPEC: the bare numeral already satisfies DASH-06's basic scope)"
  prohibitions:
    - "The recharts legitimacy checkpoint must never be auto-approved or skipped — a supply-chain trust decision requires explicit human confirmation even when the automated seam returns a verdict"
  artifacts:
    - src/components/ui/chart.tsx
    - package.json
  key_links:
    - "src/components/ui/chart.tsx is the theming wrapper the plan 05-03 funnel-chart.tsx imports; recharts entering package.json here is what makes 05-03's chart render"
---

## Phase Goal

**As a** job seeker, **I want to** have the charting dependency vetted and installed safely before any chart code is written, **so that** the analytics view can render without introducing a supply-chain risk to my local-first, privacy-sensitive tool.

_(Enabling slice for DASH-06 — separated from the analytics implementation because the blocking-human checkpoint must gate the install, per the checkpoint+implementation split rule and the Phase 3 03-01 precedent.)_

<objective>
Vet and install `recharts` (v3.x per CLAUDE.md's stack recommendation) behind the mandatory package-legitimacy checkpoint (D5-07, blocking-human), then add the official shadcn `chart` block that pulls recharts into the dependency tree. This unblocks the analytics funnel chart in plan 05-03.

Purpose: recharts is the single new attack surface Phase 5 introduces; this project's fail-loud/privacy posture requires a human supply-chain sign-off before the package lands (mirroring Phase 3's googleapis/mailparser vetting).
Output: recharts in package.json, src/components/ui/chart.tsx from the official shadcn registry, and a recorded legitimacy sign-off.

No schema changes occur in this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-analytics-dashboard-completion/05-RESEARCH.md
@.planning/phases/05-analytics-dashboard-completion/05-UI-SPEC.md
@components.json
</context>

## Artifacts this phase produces (this plan)

- `src/components/ui/chart.tsx` — shadcn chart block (`ChartContainer`, `ChartConfig`, `ChartTooltip`, `ChartTooltipContent`, `ChartLegend` as generated)
- `recharts` added to `package.json` dependencies (v3.10.1) + `package-lock.json`

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-legitimacy checkpoint for recharts (blocking-human)</name>
  <files>package.json</files>
  <action>Do NOT install anything yet. Assemble the recharts legitimacy evidence (registry identity, version, age, no-postinstall, peer deps — see how-to-verify) and HALT for explicit human approval. This checkpoint is never auto-approved (D5-07). Only after the human types "approved" may the next task run `npx shadcn add chart`.</action>
  <what-built>Nothing installed yet — this checkpoint gates the recharts install. The executor has assembled the legitimacy evidence for human review (RESEARCH Package Legitimacy Audit).</what-built>
  <how-to-verify>
    Review recharts before it is installed (this is a supply-chain trust decision that is NEVER auto-approved):
    1. Confirm the package on the official registry: visit https://www.npmjs.com/package/recharts — verify the repository resolves to `github.com/recharts/recharts`, the package is not deprecated, and weekly downloads are in the tens of millions.
    2. Confirm version + no install-time code: `npm view recharts version` (expect 3.10.1), `npm view recharts time.created` (expect 2015-08-07, i.e. 10+ years old), `npm view recharts scripts.postinstall` (expect empty — no postinstall), `npm view recharts peerDependencies` (expect react/react-dom including ^19.0.0).
    3. KNOWN false-positive note for the reviewer: `gsd-tools query package-legitimacy check --ecosystem npm recharts` returns SUS with reason "too-new". RESEARCH verified this is a heuristic false-positive keyed off the latest patch's publish date (2026-07-25), NOT the package's real 10-year age. recharts is explicitly named in this project's own CLAUDE.md stack recommendation. Do not treat the SUS signal as a genuine red flag.
    Approve only if the registry identity, version, and no-postinstall facts all check out.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize `npx shadcn add chart` (which installs recharts), or describe concerns to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install the shadcn chart block (recharts) — chart only, not progress</name>
  <files>package.json, package-lock.json, components.json, src/components/ui/chart.tsx</files>
  <read_first>
    - components.json (confirm shadcn is initialized: new-york / neutral / css-vars — reused, not re-chosen)
    - package.json (confirm recharts is absent before install; confirm react@19.2.8 satisfies recharts' peer range)
    - .planning/phases/05-analytics-dashboard-completion/05-RESEARCH.md (Standard Stack + Installation lines 82-107; Open Question 2 — skip progress)
  </read_first>
  <action>
    Only after the checkpoint above is approved: run `npx shadcn add chart` (NOT `progress` — progress is deliberately skipped per RESEARCH Open Question 2 and UI-SPEC's "planner's call: skip"). This creates src/components/ui/chart.tsx and adds recharts to package.json. Do not hand-edit package.json to add recharts — let the shadcn CLI resolve it (matches the Phase 2 02-01 decision to let the CLI resolve transitive deps rather than hand-pinning). If the CLI prompts, accept the default overwrite=no for any already-present primitive. Do not add any other package.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!(p.dependencies&&p.dependencies.recharts)) process.exit(1); console.log('recharts',p.dependencies.recharts)" && test -f src/components/ui/chart.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies include recharts (^3.x)
    - `src/components/ui/chart.tsx` exists and exports ChartContainer + ChartTooltip + ChartTooltipContent + ChartConfig
    - `src/components/ui/progress.tsx` was NOT created (progress intentionally skipped)
    - `npx tsc --noEmit -p tsconfig.json` reports no new errors introduced by the chart block
  </acceptance_criteria>
  <done>recharts is installed and src/components/ui/chart.tsx is present, ready for the 05-03 funnel chart to import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local dependency tree | A new third-party package (recharts + its transitive deps) enters the local-first, privacy-sensitive project |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | npm install of recharts via `npx shadcn add chart` | high | mitigate | Blocking-human package-legitimacy checkpoint (never auto-approved) verifies official-repo identity, 10-year age, tens-of-millions weekly downloads, and no postinstall script before the install runs; the automated SUS/"too-new" signal is a documented heuristic false-positive |
| T-05-SC2 | Tampering | Unintended extra packages pulled in (e.g. progress or a typosquat) | medium | mitigate | Install scope is limited to the single official shadcn `chart` block; progress is explicitly excluded; verify step asserts recharts is present and no progress component was created |
</threat_model>

<verification>
- Checkpoint approved before any install command runs
- recharts present in package.json; src/components/ui/chart.tsx exists; no progress.tsx
- `npx tsc --noEmit -p tsconfig.json` clean
</verification>

<success_criteria>
- recharts is vetted (human-approved) and installed; the shadcn chart wrapper is available for plan 05-03
- D5-07's blocking-human gate honored; no auto-approval
</success_criteria>

<output>
Create `.planning/phases/05-analytics-dashboard-completion/05-02-SUMMARY.md` when done.
</output>
