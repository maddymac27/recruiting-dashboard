---
phase: 04-incremental-sync-automatic-scheduling
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified: []
autonomous: false
requirements: [ING-05]
user_setup:
  - service: Windows Task Scheduler
    why: "Registering a scheduled task requires an elevated PowerShell on the user's own machine — there is no CLI/API surface Claude can drive to create a per-user scheduled task with elevation."
    dashboard_config:
      - task: "Run scripts/register-task.ps1 once in an elevated PowerShell, then confirm the registered task's settings and triggers"
        location: "Elevated PowerShell in the repo root (C:\\Users\\maddy\\recruiting-dashboard)"
must_haves:
  truths:
    - "A sync runs automatically once per day with zero action from me, including catching up on missed runs if the laptop was asleep/off (ING-05, OS/scheduling half) — the registered task fires the daily + at-logon triggers with StartWhenAvailable catch-up and no wake-timer."
  artifacts:
    - "A registered Windows scheduled task 'RecruitingDashboard-GmailSync' with StartWhenAvailable=True, WakeToRun=False, a Daily trigger, and an AtLogOn trigger"
  key_links:
    - "The registered action targets scripts/sync.ts (04-03) via tsx with the repo as working directory"
    - "A failed/expired unattended run surfaces via the 04-03 staleness banner, not silently — this is verified end-to-end here"
  prohibitions:
    - "MUST NOT enable the Task Scheduler wake-timer (WakeToRun) — D4-01 declined it deliberately to route around the hardware-dependent wake blocker; the human-verify explicitly confirms WakeToRun=False."
---

<objective>
Close the OS/scheduling half of ING-05 that cannot be unit-tested in the vitest sandbox (04-VALIDATION Manual-Only row): register the Windows scheduled task from the checked-in `scripts/register-task.ps1` (04-03) and verify — via `Get-ScheduledTask` — that it has both triggers, `StartWhenAvailable=True` (missed-run catch-up, D4-01), and `WakeToRun=False` (no wake-timer, D4-01). Optionally trigger the task once and confirm a `sync_runs` row appears (or the throttle-skip logs), proving the scheduled path reaches the same fail-loud lifecycle as the manual button.

Purpose: RESEARCH Pitfall 6 splits ING-05 into two independently verifiable claims. 04-01→04-03 proved the DATA half (a catch-up run == any large-gap incremental run) with automated tests. This plan proves the OS-CONFIG half, which is inherently manual.
Output: A live, correctly-configured scheduled task and human confirmation of its settings.

Prerequisite / carried-forward assumption: STATE.md notes the Gmail OAuth consent screen must be published to "In Production" (Phase 3 scope) so unattended refresh tokens do not expire after 7 days. If an unattended run later fails on an expired token, the 04-03 staleness banner surfaces it loudly (fail-loud, D4-03) — it does not silently stop.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-incremental-sync-automatic-scheduling/04-CONTEXT.md
@.planning/phases/04-incremental-sync-automatic-scheduling/04-RESEARCH.md
@.planning/phases/04-incremental-sync-automatic-scheduling/04-03-SUMMARY.md
</context>

<artifacts_produced>
This plan produces no new repo files — it registers and verifies OS state created from scripts/register-task.ps1 (04-03).
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Register the scheduled task and confirm catch-up-only settings + dual triggers (human-verify)</name>
  <files>scripts/register-task.ps1 (read-only — created in 04-03)</files>
  <action>
    Human-driven OS setup: run scripts/register-task.ps1 once in an elevated PowerShell, then confirm via Get-ScheduledTask that the task has StartWhenAvailable=True, WakeToRun=False, and both a Daily and an AtLogOn trigger (see how-to-verify for exact commands). Claude cannot perform this step — creating a per-user scheduled task requires interactive elevation on the user's machine.
  </action>
  <what-built>
    Claude created (in 04-03) the standalone scheduled sync script `scripts/sync.ts` and the one-time registration script `scripts/register-task.ps1` (daily + at-logon triggers, StartWhenAvailable, no WakeToRun). Registering a per-user scheduled task requires an elevated PowerShell on your machine, which Claude cannot drive — so you run the registration once and confirm the result.
  </what-built>
  <how-to-verify>
    1. Open an ELEVATED PowerShell (Run as administrator) and cd to the repo:
       `cd C:\Users\maddy\recruiting-dashboard`
    2. Register the task:
       `./scripts/register-task.ps1`
       Expect: "Registered ... RecruitingDashboard-GmailSync" with no error.
    3. Confirm the SETTINGS (D4-01 catch-up-only, no wake-timer):
       `Get-ScheduledTask -TaskName "RecruitingDashboard-GmailSync" | Select-Object -ExpandProperty Settings`
       Expect: `StartWhenAvailable : True` and `WakeToRun : False`.
    4. Confirm BOTH triggers (D4-02):
       `Get-ScheduledTask -TaskName "RecruitingDashboard-GmailSync" | Select-Object -ExpandProperty Triggers`
       Expect: one Daily trigger and one AtLogOn (logon) trigger.
    5. (Optional live smoke) Trigger it once and confirm the fail-loud lifecycle:
       `Start-ScheduledTask -TaskName "RecruitingDashboard-GmailSync"`
       Then open the dashboard sidebar (real mode) — expect the "Last synced ..." line to update to a fresh time, OR (if a sync succeeded within the last 4 hours) the run is throttle-skipped and nothing changes. Either outcome is correct.
    6. (Optional catch-up spot check, per your discretion) Leave the machine off past the daily time, then boot/log in, and confirm a new sync happens on next logon rather than being skipped.
  </how-to-verify>
  <resume-signal>Type "approved" once StartWhenAvailable=True, WakeToRun=False, and both triggers are confirmed — or paste the Get-ScheduledTask output / any error so we can adjust register-task.ps1.</resume-signal>
  <verify>
    <human-check>Get-ScheduledTask -TaskName "RecruitingDashboard-GmailSync" shows StartWhenAvailable=True, WakeToRun=False, and both a Daily and an AtLogOn trigger.</human-check>
  </verify>
  <done>The scheduled task is registered and confirmed catch-up-only (StartWhenAvailable=True, WakeToRun=False) with daily + at-logon triggers targeting scripts/sync.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| elevated PowerShell → OS scheduler | One-time registration writes a scheduled task that will later launch a node process under the user's account |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Elevation of Privilege / Tampering | registered task action (PATH/account) | high | mitigate | Registration is from the checked-in, reviewed register-task.ps1 with an explicit -WorkingDirectory; the human-verify confirms the task runs as the user (not SYSTEM) and targets scripts/sync.ts — no dynamic/network command in the action |
| T-04-10 | Denial of Service | wake-timer misconfiguration reintroduces the hardware-dependent blocker | low | mitigate | register-task.ps1 omits -WakeToRun (default False, D4-01); the human-verify step explicitly confirms WakeToRun=False after registration |
| T-04-08 | Denial of Service (of "stays accurate") | an unattended run silently failing (e.g. 7-day token expiry if OAuth left in Testing) | medium | mitigate | Carried-forward prerequisite (OAuth published to Production, Phase 3); if a run still fails, the 04-03 staleness banner + the recorded failed sync_runs row surface it loudly (D4-03) |
</threat_model>

<verification>
- Human confirms: task registered; StartWhenAvailable=True; WakeToRun=False; Daily + AtLogOn triggers present.
- Optional: Start-ScheduledTask produces a fresh sync_runs row (or a documented throttle skip).
</verification>

<success_criteria>
The scheduled task exists and is configured for catch-up-only daily + at-logon sync with no wake-timer, confirmed by direct Get-ScheduledTask inspection — closing the OS half of ING-05.
</success_criteria>

<output>
Create `.planning/phases/04-incremental-sync-automatic-scheduling/04-04-SUMMARY.md` when done.
</output>
