---
phase: 04-incremental-sync-automatic-scheduling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/db/validation.ts
  - src/db/open-sqlite.ts
  - drizzle/
  - src/gmail/types.ts
  - src/gmail/client.ts
  - src/gmail/fetch.ts
  - tests/helpers/gmail.ts
  - tests/gmail/fetch.test.ts
autonomous: true
requirements: [ING-07]
must_haves:
  truths:
    - "For a given stored cursor, the history transport returns the deduplicated set of newly-added Gmail message ids and the latest historyId, reading ONLY message ids (never subject/body/content) — the substrate that lets the incremental path match a full sync's event set (ROADMAP criterion 3)."
  artifacts:
    - "src/db/schema.ts — sync_runs.history_id (text, nullable) + sync_runs.used_fallback (boolean, not-null default false)"
    - "drizzle/<generated> — one additive migration adding both columns, applied to real and demo stores"
    - "src/db/open-sqlite.ts — PRAGMA busy_timeout = 5000 on every connection"
    - "src/db/validation.ts — newSyncRunInput extended with optional historyId + usedFallback"
    - "src/gmail/types.ts — GmailHistoryRecord, GmailHistoryListParams, GmailHistoryListResult; GmailClient.listHistory + GmailClient.getProfileHistoryId"
    - "src/gmail/client.ts — real listHistory + getProfileHistoryId implementations"
    - "src/gmail/fetch.ts — fetchHistoryMessageIds(client, startHistoryId)"
  key_links:
    - "fetchHistoryMessageIds reads history[].messagesAdded[].message.id only (never the top-level history[].messages field) — RESEARCH Pitfall 1"
    - "The additive migration lands in drizzle/ so createTestDb()'s runMigrations() exposes the columns to every later test"
  prohibitions:
    - "MUST NOT fetch, log, or transmit email subject/body/content in the history transport — message ids and the numeric historyId only (D3-01: nothing leaves the machine beyond the existing gmail.readonly calls)."
---

<objective>
Lay the two lowest layers of the incremental-sync slice: (1) the `sync_runs` cursor columns (`history_id`, `used_fallback`) via one additive Drizzle migration plus the `busy_timeout` pragma that makes the scheduled script and a running `next dev` server safe to write concurrently, and (2) the Gmail `history.list` / `getProfile` transport and the `fetchHistoryMessageIds` pagination helper that turns a stored cursor into a deduplicated list of newly-added message ids.

Purpose: These are the persistence + transport substrate that ING-07's cursor-expiry fallback and ROADMAP criterion 3's incremental-parity behavior sit on. This plan does not yet change `runGmailSync` — it makes the columns exist and proves the transport reads only message ids, deterministically, against a fake Gmail client.
Output: Applied migration (columns exist in real + demo stores), extended zod schema, new Gmail transport methods, and a green `fetchHistoryMessageIds` test.

MVP observable effect (backend phase): a passing `fetchHistoryMessageIds` unit test proving cursor→deduped-ids behavior, plus the migration applied so `sync_runs` physically carries the cursor.

Flagged assumption (spec-probe): the deterministic edge probe left ING-07 `unclassified/unresolved`; acceptance criteria here are derived directly from ROADMAP success criterion 3 and RESEARCH Pattern 1, not from an auto-shaped SPEC edge.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-incremental-sync-automatic-scheduling/04-CONTEXT.md
@.planning/phases/04-incremental-sync-automatic-scheduling/04-RESEARCH.md
@.planning/phases/04-incremental-sync-automatic-scheduling/04-PATTERNS.md
</context>

<artifacts_produced>
NEW symbols/files this plan creates (exclude from drift/no-orphan checks):
- Columns: `sync_runs.history_id`, `sync_runs.used_fallback`
- Migration: one new folder under `drizzle/` (drizzle-kit generated slug)
- Types: `GmailHistoryRecord`, `GmailHistoryListParams`, `GmailHistoryListResult`
- Interface methods: `GmailClient.listHistory`, `GmailClient.getProfileHistoryId`
- Function: `fetchHistoryMessageIds` (src/gmail/fetch.ts)
- Zod fields: `newSyncRunInput.historyId`, `newSyncRunInput.usedFallback`
- Test file: `tests/gmail/fetch.test.ts` history describe block; extended `FakeGmailFixtures`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the Gmail history/profile contract and write the failing fetchHistoryMessageIds test (RED)</name>
  <files>src/gmail/types.ts, tests/helpers/gmail.ts, tests/gmail/fetch.test.ts</files>
  <read_first>
    - src/gmail/types.ts (analog: existing GmailListMessagesParams/GmailListMessagesResult/GmailClient, PATTERNS lines 156-174)
    - tests/helpers/gmail.ts (analog: makeFakeGmailClient / FakeGmailFixtures, already in context)
    - tests/gmail/fetch.test.ts (existing pagination test shape for listAllMessageIds)
    - 04-PATTERNS.md §src/gmail/types.ts and §tests/helpers/gmail.ts (fixture guidance)
  </read_first>
  <behavior>
    - fetchHistoryMessageIds(fakeClient, "1000") returns every id in history[].messagesAdded[].message.id across ALL pages, deduplicated (a Set), and the latest non-empty historyId seen.
    - Ids that appear ONLY in a history record's top-level `messages` array (not in messagesAdded) are NOT returned (RESEARCH Pitfall 1).
    - Multi-page: a fixture spanning >1 page (via nextPageToken) returns the union of all pages' messagesAdded ids.
    - Empty history (no messagesAdded) returns [] and echoes back the startHistoryId as newHistoryId.
  </behavior>
  <action>
    Extend src/gmail/types.ts following the exact doc-comment + narrow-shape convention already in the file: add GmailHistoryRecord ({ messagesAdded?: { message?: { id?: string | null } }[] }), GmailHistoryListParams ({ startHistoryId: string; pageToken?: string }), and GmailHistoryListResult ({ history: GmailHistoryRecord[]; historyId?: string; nextPageToken?: string }); extend the GmailClient interface with `listHistory(params: GmailHistoryListParams): Promise<GmailHistoryListResult>` and `getProfileHistoryId(): Promise<string | undefined>`. Add a comment citing RESEARCH Pitfall 1 explaining the messagesAdded-only read and ING-07/criterion-3 as the driver.

    Extend tests/helpers/gmail.ts FakeGmailFixtures with: `historyByStartId?: Record<string, GmailHistoryListResult[]>` (each entry is the full ordered list of pages returned for a startHistoryId; the fake slices by an offset pageToken exactly like messagesByQuery does), `profileHistoryId?: string`, and `rejectListHistory?: unknown` (when set, the fake's listHistory rejects with this value — used by 04-02 to inject a real Common.GaxiosError). Implement fake `listHistory` (page through the fixture, honoring rejectListHistory first) and `getProfileHistoryId` (return profileHistoryId). Do NOT duck-type the rejection here; 04-02 supplies a real GaxiosError instance so the instanceof guard is genuinely exercised.

    Create tests/gmail/fetch.test.ts additions (or a new describe block) importing fetchHistoryMessageIds from @/gmail/fetch and makeFakeGmailClient. Write the four behavior cases above. The test is RED now: fetchHistoryMessageIds does not exist yet (import resolves to undefined / throws), so the suite fails.
  </action>
  <verify>
    <automated>npx vitest run tests/gmail/fetch.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/gmail/fetch.test.ts` runs and REPORTS FAILURE (RED) specifically because `fetchHistoryMessageIds` is not yet implemented — not because of a fixture/type authoring error.
    - GmailClient interface now declares `listHistory` and `getProfileHistoryId`; the fake client in tests/helpers/gmail.ts implements both.
    - No test reads or asserts on email subject/body content — ids and historyId only.
  </acceptance_criteria>
  <done>The history transport contract and its failing test exist; the fake Gmail client can serve paged history fixtures and reject on demand.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Add sync_runs cursor columns + busy_timeout pragma, generate and apply the additive migration</name>
  <files>src/db/schema.ts, src/db/validation.ts, src/db/open-sqlite.ts, drizzle/</files>
  <read_first>
    - src/db/schema.ts (analog: syncRuns table, PATTERNS lines 336-354; and the nullable-additive-column comment at schema lines ~228-232)
    - src/db/validation.ts (analog: newSyncRunInput, PATTERNS lines 363-379)
    - src/db/open-sqlite.ts (analog: existing foreign_keys/journal_mode pragma block, PATTERNS lines 388-398)
    - tests/db/schema-parity.test.ts (the gate that proves migration == schema)
    - drizzle/20260730193220_silent_aqueduct (the Phase 3 additive-migration precedent)
  </read_first>
  <action>
    Per D4-05 (cursor carried on the successful sync_runs row) and RESEARCH open-question 2 (dedicated used_fallback boolean): add two additive columns to the syncRuns table in src/db/schema.ts — `historyId: text("history_id")` (nullable) and `usedFallback: integer("used_fallback", { mode: "boolean" }).notNull().default(false)`. Additive-nullable/defaulted only, matching the Phase 3 pattern so the migration applies cleanly to a non-empty real.sqlite.

    Extend newSyncRunInput in src/db/validation.ts with `historyId: z.string().min(1).optional()` and `usedFallback: z.boolean().optional()` (same optional-field convention as the existing fields), so finishSyncRun (04-02) validates the new fields via V5 rather than bypassing zod.

    Add `sqlite.exec("PRAGMA busy_timeout = 5000")` to openSqliteFile in src/db/open-sqlite.ts alongside the existing pragmas (RESEARCH Pitfall 4) — one centralized fix covering client.ts, migrate.ts, seed scripts, and the future scripts/sync.ts, so a scheduled sync and a manual "Sync now" writing at the same instant wait briefly instead of failing with SQLITE_BUSY.

    Generate the migration with `npm run db:generate` (drizzle-kit generate — do NOT hand-write SQL). Then APPLY it to BOTH stores so they stay schema-consistent, mirroring prior additive migrations: `DASHBOARD_MODE=real npm run db:migrate` and `DASHBOARD_MODE=demo npm run db:migrate`. This step is BLOCKING: the columns must physically exist before 04-02's tests read them — a type/build check alone passes on config-derived types even when the live DB lacks the columns (false-positive verification).
  </action>
  <verify>
    <automated>npm run db:generate && DASHBOARD_MODE=real npm run db:migrate && DASHBOARD_MODE=demo npm run db:migrate && npx vitest run tests/db/schema-parity.test.ts tests/db/open-sqlite.test.ts tests/db/validation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A new folder exists under `drizzle/` whose SQL adds `history_id` and `used_fallback` to `sync_runs` (ALTER TABLE ADD COLUMN, additive).
    - `npm run db:migrate` applies cleanly to both real and demo stores with no error.
    - `tests/db/schema-parity.test.ts` passes (migrated DB matches the Drizzle schema).
    - openSqliteFile sets `busy_timeout = 5000`; `tests/db/open-sqlite.test.ts` passes.
    - `newSyncRunInput` accepts (and strips-unknown-safely validates) historyId + usedFallback; `tests/db/validation.test.ts` passes.
  </acceptance_criteria>
  <done>sync_runs carries history_id + used_fallback in both stores; every connection sets busy_timeout; the zod schema admits the new fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the Gmail history transport (GREEN)</name>
  <files>src/gmail/client.ts, src/gmail/fetch.ts</files>
  <read_first>
    - src/gmail/client.ts (analog: existing listMessages/getMessageRaw/listLabels, PATTERNS lines 111-146)
    - src/gmail/fetch.ts (analog: listAllMessageIds, PATTERNS lines 183-197)
    - 04-RESEARCH.md §Code Examples (listHistory/getProfileHistoryId real impl, lines 443-468) and §Pattern 1 (fetchHistoryMessageIds, lines 214-239)
  </read_first>
  <behavior>
    - Makes the RED test from Task 1 pass: fetchHistoryMessageIds paginates client.listHistory, collecting messagesAdded ids into a Set and tracking the latest historyId.
    - client.listHistory calls gmail.users.history.list with historyTypes ["messageAdded"] and maxResults 500, mapping the raw response into the narrow GmailHistoryListResult (messagesAdded only), never returning raw googleapis objects.
    - client.getProfileHistoryId calls gmail.users.getProfile and returns res.data.historyId ?? undefined.
    - Neither method catches its own GaxiosError — a 404 (expired cursor) propagates unchanged to the caller (the fallback decision lives in ingestion.ts, 04-02).
  </behavior>
  <action>
    Add listHistory and getProfileHistoryId to wrapGmailClient in src/gmail/client.ts following the exact shape of the existing methods (params destructured at top level, results mapped into the narrow GmailClient-interface shape). Use the verified impl in 04-RESEARCH.md §Code Examples (lines 443-468) — historyTypes ["messageAdded"], maxResults 500, map history[].messagesAdded, echo historyId/nextPageToken. Do NOT add a try/catch: let GaxiosError propagate (PATTERNS line 148).

    Add fetchHistoryMessageIds(client, startHistoryId) to src/gmail/fetch.ts following the do/while pagination shape of listAllMessageIds and the code in 04-RESEARCH.md §Pattern 1 (lines 219-239): accumulate history[].messagesAdded[].message.id into a Set, advance pageToken, track latestHistoryId, return { messageIds: [...set], newHistoryId }. Read messagesAdded only (never the top-level messages field) — cite RESEARCH Pitfall 1 in the doc comment. Add ING-07 / criterion-3 citations to both new functions per the project's doc-comment-traceability convention (PATTERNS §Shared Patterns).
  </action>
  <verify>
    <automated>npx vitest run tests/gmail/fetch.test.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/gmail/fetch.test.ts` PASSES (all four history behavior cases green).
    - `npx tsc --noEmit` reports no errors (the real client.ts now satisfies the extended GmailClient interface).
    - src/gmail/client.ts listHistory sends `historyTypes: ["messageAdded"]` and contains no try/catch around the googleapis call.
    - fetchHistoryMessageIds references `messagesAdded` and does not read `res.history[].messages`.
  </acceptance_criteria>
  <done>fetchHistoryMessageIds turns a cursor into a deduped id list against the real client interface; the transport reads ids only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transport → Gmail API (HTTPS) | New history.list / getProfile calls cross to Google's readonly API; untrusted response data returns |
| schema/zod → SQLite | New cursor columns written from validated input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | fetchHistoryMessageIds / client.listHistory / getProfileHistoryId | low | mitigate | Transport reads only `messagesAdded[].message.id` and the numeric `historyId`; no subject/body/content is fetched or logged — preserves D3-01 (nothing new leaves the machine; still within the existing gmail.readonly scope) |
| T-04-02 | Tampering | sync_runs.history_id / used_fallback write path | low | mitigate | New columns flow through `newSyncRunInput` (zod, V5) before Drizzle `.set()`; columns are additive-nullable/defaulted so the migration is non-destructive to the real store |
| T-04-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs occur this plan or phase — RESEARCH Package Legitimacy Audit confirms zero new dependencies (googleapis/tsx/drizzle-kit already installed); no supply-chain surface added |
</threat_model>

<verification>
- `npx vitest run tests/gmail/fetch.test.ts` green (transport parity substrate).
- `npx vitest run tests/db/schema-parity.test.ts tests/db/open-sqlite.test.ts tests/db/validation.test.ts` green.
- `npx tsc --noEmit` clean.
- Manual spot check: the generated drizzle/ SQL is ADD COLUMN only (no DROP/rename).
</verification>

<success_criteria>
sync_runs physically carries history_id + used_fallback in both stores, busy_timeout is set on every connection, and fetchHistoryMessageIds deterministically maps a cursor to a deduplicated, content-free message-id set.
</success_criteria>

<output>
Create `.planning/phases/04-incremental-sync-automatic-scheduling/04-01-SUMMARY.md` when done.
</output>
