# Dependencies
node_modules/

# Next.js
.next/
out/

# Local data — real job-search data must never enter this repo
data/*.sqlite
data/*.sqlite-*

# Environment / secrets
.env
.env.*
!.env.example

# Google OAuth credentials & tokens (Phase 3) — never commit
.secrets/
client_secret*.json
*.credentials.json
google-credentials*.json
token*.json
*.tokens.json

# Research cache (transient web-fetch cache — noise, not published)
.planning/research/.cache/

# Misc
*.log
.DS_Store
*.tsbuildinfo
